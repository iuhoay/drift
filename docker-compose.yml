services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ${USER}
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - drift_pgdata:/var/lib/postgresql/data

volumes:
  drift_pgdata:
