# AGENTS.md

This project keeps a single source of truth for AI-agent guidance in **[CLAUDE.md](./CLAUDE.md)** —
stack, commands, domain model, and conventions. Read that file.

It's canonical for every agent (Codex included); this stub only exists so tools that look for
`AGENTS.md` find their way there. Don't duplicate guidance here — update `CLAUDE.md` instead.
