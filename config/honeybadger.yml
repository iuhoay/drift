---
# For more options, see https://docs.honeybadger.io/lib/ruby/gem-reference/configuration

api_key: "<%= ENV['HONEYBADGER_API_KEY'] %>"

# The environment your app is running in.
env: "<%= Rails.env %>"

# The absolute path to your project folder.
root: "<%= Rails.root.to_s %>"

# Honeybadger won't report errors in these environments.
development_environments:
- test
- development
- cucumber

# By default, Honeybadger won't report errors in the development_environments.
# You can override this by explicitly setting report_data to true.
# report_data: true

# The current Git revision of your project. Defaults to the last commit hash.
# revision: null

# Enable verbose debug logging (useful for troubleshooting).
debug: false

# Error reporting only — Insights (events/metrics and its plugins) stays off.
# Besides being unused (Rails Pulse covers perf), its solid_queue plugin
# force-loads ActiveRecord::Base mid-boot, tripping Rails' premature-load-hook
# warning on non-eager-loaded boots such as `kamal reindex`; see
# https://github.com/honeybadger-io/honeybadger-ruby/issues/811.
insights:
  enabled: false
