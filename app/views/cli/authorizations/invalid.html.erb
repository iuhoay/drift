<% content_for :title, "Authorize CLI" %>

<p class="text-sm">
  <span class="text-amber-700">drift</span><span class="text-stone-400">:</span><span class="text-stone-500">~/cli</span><span class="term-caret select-none" aria-hidden="true"></span>
</p>
<h1 class="mt-4 text-base font-semibold tracking-tight text-stone-900">This authorize link is invalid.</h1>
<p class="mt-3 text-sm text-stone-600">
  Run <code class="font-mono text-stone-800">drift auth login</code> again from the CLI.
  The redirect must be a loopback address on this machine.
</p>
